import React from 'react';
import { StyledFooter } from '../styles/Footer';

const Footer = () => {
  return (
    <StyledFooter>
      <h1>Footer</h1>
    </StyledFooter>
  );
};

export default Footer;
